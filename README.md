# photomap
Extract location metadata from Apple Photo Library

Command line tool to extract location metadata from Apple Photo Library.

Produces a CSV file with location data for each photo in the library.

## Usage
To use photomap, run the following command:
```bash
photomap > locations.csv
```
This will generate a CSV file named `locations.csv` in the current directory containing the location data for each photo in the library.
